@model MusicDating.Models.Entities.Ensemble

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Ensemble</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EnsembleName" class="control-label"></label>
                <input asp-for="EnsembleName" class="form-control" />
                <span asp-validation-for="EnsembleName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EnsembleDescription" class="control-label"></label>
                <input asp-for="EnsembleDescription" class="form-control" />
                <span asp-validation-for="EnsembleDescription" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
